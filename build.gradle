plugins {
    id 'groovy'
    id 'maven'
}

group = 'me.omico.gradle'
version = '1.0.0'

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocal().url)
        }
    }
}
